import { SampleDirectiveDirective } from './sample-directive.directive';

describe('SampleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SampleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
